# Moresheth — Backlog

> Maintained collaboratively. Update priorities here at the start of each session.
> Status: 🔴 Bug/Blocker · 🟡 High Priority · 🔵 Queued · 💡 Nice-to-Have

---

## 🔴 Active Bugs / Needs Verification

| # | Item | Notes |
|---|------|-------|
| B1 | **Challenges: NOT NULL constraint errors** | `product_id`, `required_spend`, `reward_value` — migration deployed; verify no new columns surface |
| B2 | **Challenges: "cannot load challenge" error** | `db.rollback()` fix deployed; verify challenge list loads cleanly |
| B4 | **Backend: slow requests under concurrent load** | Page loads trigger a synchronous full Plaid sync across all banks; DB connection pool backed up to 90-100s+ response times during v2.html QA pass. Needs profiling — likely pool size or sync-on-every-load pattern. |
| B5 | **`/api/cash-flow` returns all zeros** | Cash Flow page (both frontend.html and v2.html use the shared endpoint) shows $0 income/expenses/breakdown for every month and every range toggle (This Month/Last 30/Next 30), despite plenty of transaction activity. Likely scoped to checking/savings only and either the scoping logic or the underlying data tagging is off. Not a v2.html-specific bug — pre-existing backend issue. |
| B7 | **Venmo mis-tagged "Healthcare" budget category** | A stray auto-created rule (`id=504`, "Auto-created from LLM enrichment") sets `set_category='Healthcare'` for any VENMO-pattern transaction — 5 rejections, 0 acceptances in its history, suggesting it's been silently wrong and manually corrected repeatedly. Found 2026-07-18 while adding the P2P Payments CSC. **Still not fixed** — now also visible directly in the Rules table (Settings → Rules) as "5m 0a 5r ⚠". A second, previously-undocumented bad Venmo rule was also found at priority 340 ("9m 0a 9r ⚠") — see B8. |
| B8 | **Rule cleanup pass — several rules with 100% rejection history** | Found 2026-07-18 once `times_rejected` was surfaced in the Rules table (Settings → Rules → STATS column, red ⚠ when rejected > accepted). Confirmed still active with 0 acceptances against real rejections: priority-340 Venmo rule (9m 0a 9r, duplicates B7's problem), `AUTOPAY` action rule (8m 0a 8r), `CHASE CREDIT CRD` (3m 0a 3r), `CITI AUTOPAY;CITI CARD ONLINE PAYMENT` (2m 0a 2r), `VERIZON` (2m 0a 2r), `TRANSFER` description rule (2m 0a 2r). Needs a human pass — each rule needs its own judgment call (fix pattern vs. deactivate vs. leave as duplicate-but-harmless), not a blanket delete. |
| B9 | **Rule overlap audit — 12 pairs need a manual priority/pattern fix** | Found 2026-07-18 via the new `find_overlapping_rules()` audit (part of the classification-engine redesign, see PLAN.md). 20 overlapping-pattern pairs found total; 8 are already correctly resolved today (specific pattern already has the lower priority number) and need no action. The other **12 need a manual fix, none of them self-resolving**: **8 "general pattern accidentally beats a more specific one by an adjacent priority number"** bugs — `UBER`(5233) beats `UBER EATS`(5234), `KINGS`(5120) beats `KINGS OF KOBE`(5121), `MOUNT SINAI`(5142) beats `MOUNT SINAI PARKING`(5143), `COSTCO`(5064) beats `COSTCO GAS`(5065), `HILTON`(5100) beats `HILTON RESORT CREDIT`(5101), `CC PAYMENT`(40) beats `CHASE CC PAYMENT`(200, id 524), and `HOT`(5107, a coincidental substring) beats both `TEANECK ROAD HOT BAGELS`(5218) and `WDW PHOTOPASS`(5246) — **correction to the note originally logged here**: a "prefer more specific pattern" reorder was tested directly during the redesign and confirmed to fix *none* of these — it only breaks ties between rules sharing the exact same priority number, and these all involve different (adjacent) priorities, so each needs its specific priority number changed by hand. Plus **3 true contradictions**: two `CHASE CC PAYMENT` rules (id 14 → Transfer vs id 524 → Fees & Interest — 524 is dead), two `AUTOMATIC PAYMENT` rules (id 528 → Transfer vs id 529 → Expense — 529 is dead), `ALIGNED INCENTIV` vs `ALIGNED INCENTIVES` (id 500 vs id 65, looks like a typo variant). Plus **1 same-priority tie**: `SCHEELS ALL SPORTS` vs `SCHEELS ALL SPORTS IFARGO` (ids 522/523, both priority 200, currently resolved by non-deterministic Postgres row order). |
| B10 | **Card→product auto-suggestion algorithm gives bad matches** | Found 2026-07-19 while investigating why "Amex BBP 1008" (account 135) was missing from every ecosystem drill-down (see Completed). The `/api/accounts/product-suggestions` fuzzy-matcher suggested "Amex Platinum" for it at only medium confidence, even though an exact catalog match (`amex_blue_business_plus`, official_name "Blue Business Plus Card") already existed — that's likely why the suggestion was never confirmed, leaving the account permanently unlinked. **2026-07-20: found and fixed 3 more instances of the same pattern** (see Completed) — United Explorer 9968 (suggested "United Quest" instead of the near-exact "United℠ Explorer Card" match) and both BOA Cash Rewards accounts (7384, 8344 — both suggested "Fidelity Rewards Visa" instead of the exact "Bank of America Customized Cash Rewards" match). All 4 now manually linked to their correct products. **Still not fixed: the scoring function itself** — it's not surfacing exact/near-exact official_name matches over generic ones, so this class of bug will keep recurring for any newly-linked account until the algorithm itself is fixed. One account (CapitalOne VentureOne 9689) surfaced during the 2026-07-20 audit has no correct suggestion available at all since no matching catalog product exists — see B14. |
| B11 | **`earn-detail` (ecosystem drill-down) has no `auto_top_category` handling** | Found 2026-07-19 while fixing B-earn-summary-divergence (see Completed). `/api/cards/earn-summary` special-cases `auto_top_category` products (e.g. Citi Custom Cash's 5%-on-top-category) via `calc_auto_top_category_points()`, but `/api/ecosystems/{id}/earn-detail` (main.py:5069) has no equivalent branch — it just applies `compute_points_earn()` with the card's base rate, silently ignoring the auto-top bonus for any such card's ecosystem drill-down page. Not fixed this session (out of scope — found while making the two endpoints agree on the non-auto-top case). Whichever endpoint is "correct" should be made authoritative for both, ideally via a shared helper so this class of drift can't recur. |
| B12 | **Benefits & Credits summary mixes annual/period basis in "remaining"** | Found 2026-07-20 on the Bonvoy Brilliant card page. The card-level summary line ("$X annual value · $Y used · $Z remaining") sums `annualValue(b)` (full annualized value, e.g. $300/yr for a $25/mo credit) into "annual value" but sums only the *current period's* `remaining` (e.g. $25, since the monthly credit resets monthly) into "remaining" — so the two numbers are on different bases and don't reconcile (e.g. "$520 annual value" vs "$245 remaining" looked like $275 was unaccounted for). Not a data bug, just a confusing display — flagged to the user, not fixed (out of scope for the card-level session that found it). Would need either an amortized "remaining this year" calc across all periods, or relabeling to make the period mismatch explicit. |
| B13 | **`v2.html` references undefined `--violet`/`--violet-soft`/`--violet-border` CSS tokens** | Found 2026-07-20 while fixing the Dashboard budget bars. Several places (e.g. the challenge-bonus panel around line 5587) use `var(--violet)` etc., but no `[data-theme]` block actually defines these custom properties — they silently resolve to nothing in both themes. Not fixed this session (spawned as a separate flagged task); the new "no budget set" bar state deliberately used a neutral gray instead of reaching for the broken token. |
| B14 | **No `CardProduct` catalog entry for Capital One VentureOne** | Found 2026-07-20 during the unlinked-cards audit (see B10/Completed). Account "CapitalOne VentureOne 9689" (official_name "VentureOne") has no correct catalog match — only "Capital One Venture" (id 28) and "Capital One Venture X" (id 27) exist, both different products with different earn rates/benefits from VentureOne. Needs a new `CardProduct` row (with its own `CardProductReward` rates) created before this account can be linked and show up in any ecosystem drill-down — a card-research task, not just a data-linking fix. |
| B15 | **Capital One Miles doesn't appear as an ecosystem tile on Portfolio** | Flagged 2026-07-25. The "Capital One Miles" ecosystem exists in `PointsEcosystem` (id 4), but no Capital One tile shows up on the Portfolio grid — likely because no Capital One account currently has a linked `CardProduct` (ties directly to B10/B14: VentureOne has no catalog match, and Venture/Venture X may simply not be linked to any held account yet). An ecosystem only surfaces in `/api/cards/earn-summary` if at least one account's `acct_info` resolves an `eco_id`. Needs: confirm which Capital One account(s) exist, link/create the right `CardProduct` match, verify the tile appears. |
| B16 | **Some card thumbnails crop too aggressively** | Flagged 2026-07-25 on the "Your Cards" tile grid (180×114, `object-fit:cover`). Most `static/cards/*.png` are close to the target 1.587:1 card ratio and crop fine, but at least one (`us_bank_cash_plus.png`, 384×609 — **portrait**, not landscape) would get almost entirely cropped away by a landscape tile. Needs an audit of every file in `static/cards/` for aspect-ratio mismatches (not just the ones touched this session) and a reprocess pass (crop/pad to ~1.587:1) for any that are off, similar to the crop treatment already applied to the AI-generated Chase/Citi/Capital One art. |
| B17 | **Challenge cards inconsistent: progress bar only shows for `spend_threshold` challenges, not `spend_cap` ones** | Flagged 2026-07-25. `ChallengeCard` (v2.html) has two mutually-exclusive render branches: `isThreshold` (spend_threshold-based) gets a progress bar with spent/goal/to-go; `bonus_type==='per_dollar'` with a `spend_cap` (e.g. "Q3 5x Groceries" on Bonvoy Boundless ···3663) only gets a flat "+5x on [Categories] · $spent/$cap" line with **no progress bar**. Also, the header subtitle already lists `category_names` for any challenge that has them, which then get **repeated** in the "+Nx on [Categories]" bottom line for per_dollar ones — redundant. **Fix**: (1) show the same progress-bar treatment (spent/goal/to-go) for spend_cap challenges too, using `spend_cap` as the goal; (2) drop `category_names` from the header subtitle entirely, since it's already shown in the "+Nx on [Categories]" line where applicable — the header should just be card name + last four, matching how non-category challenges already look. |

---

## 🟡 High Priority

| # | Item | Notes |
|---|------|-------|
| H1 | **Mobile QA pass** | User working through mobile-specific bugs — awaiting findings |
| H2 | **Account reclassification** | User should reclassify FSA/HSA accounts from "Other Assets" to correct type using the new dropdown |
| H3 | **Transaction: filter by description/merchant** | Text search input — already exists; verify working well |
| H4 | **Challenge: verify recalc correctness** | Confirm spend from `activation_date` → today captured correctly |
| H5 | **Card Research Skill — capture benefits** | Extract full benefit list and auto-POST to `/api/card-products/{id}/benefits` |

---

## 🔵 Features — Queued

| # | Item | Notes |
|---|------|-------|
| F1 | **Classify merchant with AI** | "Ask AI" button next to null-CSC merchants — Claude web-searches MCC/category → saves as merchant mapping |
| F2 | **Plaid re-link safety** | No duplicate transactions or broken accounts when re-linking a Plaid item |
| F3 | **Benefits: auto-track from transactions** | If a benefit has `trigger_category`, auto-update `amount_used` from matching transactions |
| F4 | **Challenges: suggestions / templates** | "Suggested challenges for your cards" — backend exists, wire to UI |
| F5 | **Visa/MC merchant category API** | Use Visa Supplier Locator API to look up merchant MCCs; map MCC → CSC |
| F6 | **Network-specific CSC overrides** | Per-network merchant mapping for earn rate accuracy |
| F7 | **Points valuation** | CPP multiplier per ecosystem to show estimated $ value alongside raw points |
| F8 | **Add Ritz-Carlton `CardProduct` + run the real Boundless→Ritz-Carlton migration** | The `POST /api/cards/{card_id}/change-product` feature (2026-07-25) is ready, but there's no Ritz-Carlton entry in the card-products catalog yet — a concurrent session's stopgap only swapped the card-art image on the existing `marriott_bonvoy_boundless` product_key. Needs a card-research pass for Ritz-Carlton's real earn rates/benefits, then run `change-product` for real on the two actual Bonvoy Boundless cards (card_id 31, 32) with the real effective date. |
| F9 | **Card thumbnail redesign — last-4-digits badge** | Requested 2026-07-25. Replace the current plain white "···1234" text sitting directly on the card art with a small rounded-rectangle badge in the bottom-left corner, background at ~10% opacity so it stays legible without a hard block, and drop the "···" prefix (just the 4 digits) — the badge's own boundary already signals "this is the number," so the "···" convention (meant to imply "redacted digits before this") becomes redundant. Prevents the digits from visually blending into busy/bright card art underneath (the current failure mode this is meant to fix). |

---

## 💡 Nice-to-Have / Phase 2

| # | Item | Notes |
|---|------|-------|
| ~~N1~~ | ~~**PWA v2**~~ | ✅ Pull-to-refresh, swipe actions, skeleton loading, virtual scroll, offline support — all shipped |
| N2 | **Component refactor** | Extract frontend.html into proper component files |
| N3 | **Export to CSV/PDF** | CSV export exists; add PDF support for statement reconciliation |
| N4 | **AI-driven UI polish** | Use v0/Lovable for isolated components once functionality stable |

---

## ✅ Recently Completed

- **Accounts: "+ Add Account" button for healthy bank connections (2026-07-26)** — Omer added new checking accounts and had no way to link them to an already-connected bank without going through error-recovery. The Plaid update-mode Link flow already existed but was only reachable via the "🔗 Reconnect" button, itself only shown when `item.last_error_code==='ITEM_LOGIN_REQUIRED'`. Added a general "+ Add Account" button (same `openReconnect` logic) to every healthy bank row in Settings → Bank Links. No backend changes needed — `/api/plaid/update-link-token/{item_id}` was never actually gated on error state. See PLAN.md for the full writeup. Follow-up: this is also the first real use of F2 (Plaid re-link duplicate-safety) — worth verifying once Omer actually runs it against his new checking accounts.
- **Card product changes with locked, point-in-time points (2026-07-25)** — new `CardProductHistory` model (effective-dated, mirrors `TransferRatio`) plus `points_earned`/`points_earn_classification`/`points_earn_rate`/`points_product_id`/`points_locked_at` on `Transaction`. Points are now computed once at write time (sync/import/manual-create, and re-locked on category/action/exclude/override edits, always resolving whichever product was active on the transaction's own date) and never recomputed live on read — this is what lets a card's product change without retroactively altering historical earn totals. New `POST /api/cards/{id}/change-product` + `GET /api/cards/{id}/product-history`, plus a "Card Product" section on the card detail page (current product, Change Product modal, product history list). One-time startup backfill locked all 2,199 existing transactions. See PLAN.md for the full design writeup and a note on a concurrent-session schema surprise encountered mid-session. Follow-up: F8 (Ritz-Carlton catalog entry + the real migration for the two actual cards).
- **App-wide UI design-system pass — H6 closed (2026-07-25)** — `v2.html` only. Four asks plus a follow-through on the gold→blue palette gap found along the way:
  1. **Subtle borders on all buttons** — `.btn`/`.btn-primary`/`.btn-secondary`/`.btn-ghost`/`.btn-success` (global CSS) now carry a theme-tinted 1px border matching the existing GCB/Rule row-action pattern; also added matching borders to 7 ad-hoc colored buttons that had `border:'none'` (delete-account, Settings Fresh Start/Nuke Everything, card-detail Back button, 3 Ecosystem inline ✓ confirm buttons).
  2. **Duplicate page titles removed** — Dashboard/Accounts/Budgets/Cash Flow/Daily Balances all had two headings (topbar + an in-page `<h2>` repeating the same name above the KPI cards). Removed the in-page duplicate on Accounts/Budgets/Cash Flow/Daily Balances (Dashboard's "Good morning, Omer" greeting kept — not a true duplicate); `.topbar-title` restyled to the nicer 20px/400-weight/-0.3px style the removed headings used; the `titles` map promoted two entries to their fuller text (`cashplanner` → "Cash Flow & Forecasting", `cashflow` → "Daily Balance Timeline") per user's choice, sidebar nav labels left short.
  3. **Top-of-page nav rows normalized to the Budgets pattern** (title/tabs left, pills/selectors right, wrapped in one `.card`) — Dashboard, Settings, Cards, Net Worth, GCB, Daily Balances (both its tab-switcher and its range-selector row) were previously bare `<div>`s with no card container; Net Worth's sub-tabs were also duplicated twice in the source (dead leftover) — removed.
  4. **No page-level horizontal scroll** — audited; the only genuinely wide content (Budgets' 1100-1200px annual/edit-year tables) already scrolls contained within a `padding:0, overflow:hidden` card, confirmed via `document.documentElement.scrollWidth` at both desktop and tablet widths (768px) — no code change needed there.
  5. **Gold→blue palette pass (full, not just nav)** — found `var(--gold)`/`var(--gold-soft)` still used in ~70 places (tab underlines, KPI figures, chart lines/gradients, points/reward badges); `--gold-soft` was never even defined (same class of bug as the known `--violet`/`--violet-soft` gap, B13) so those backgrounds were silently invisible. User chose the full app-wide pass over nav-only. Replaced every `var(--gold)`→`var(--blue-primary)` and `var(--gold-soft)`→`rgba(var(--blue-primary-rgb),0.12)`; fixed 3 non-`var()` call sites separately (a `getComputedStyle('--gold')` JS read, a hardcoded `#e8c060` gradient stop, a Loans payoff-timeline categorical chart palette where blue-primary would have collided with an existing blue swap — used `#06b6d4` cyan instead for distinctness). Removed the now-dead `--gold` CSS variable (kept `--amber`, a separate token with the same hex that's still legitimately used for warning states). Left untouched: `amex_gold`/`delta_gold` card-art gradient keys (real card brand colors, not theme) and traffic-light-style hardcoded `#f59e0b` warning colors unrelated to the nav theme.
  - Verified live (local dev server) across all 11 pages in both dark and light theme; no console errors, no page-level horizontal overflow at 1400px/768px.

- **Card art wired in + fixed a systemic product_key naming-mismatch bug (2026-07-24)** — `static/cards/*.png` used short filenames (`sapphire_preferred.png`, `world_of_hyatt.png`...) that never matched the real `CardProduct.product_key` values (`chase_sapphire_preferred`, `hyatt_personal`...) the code looks up in both places that render card art (`EcosystemDetailPage` and the Portfolio page's "Your Cards" tiles) — confirmed via a wall of 404s in server logs; all card art, old and new, was silently falling back to a plain gradient. Fixed by renaming existing real-art files to their correct key and correcting both `cardGrads` dicts. Wired in 7 exact-match + 2 renamed new images from `assets/card_logos_color/`, plus added 3 new Citi Strata-family `CardProduct` catalog entries (Strata/Strata Premier/Strata Elite — Citi's 2025 rebrand, verified real rates via web search, not memory) since those images didn't match anything in the catalog. Verified live.
- **Portfolio tile vs. drill-down balance mismatch fixed, all ecosystems (2026-07-24)** — Reported against Hilton (tile showed 13,906, drill-down showed 25,949) but was systemic: the Portfolio tile showed period-earned points (`/api/cards/earn-summary`) while the drill-down's Current Balance is the all-time snapshot-anchored ledger figure — two different metrics that happened to occupy the same spot, not a bug in either number individually. Extracted the balance math into a shared `_compute_ecosystem_balance()` helper (`main.py`) used by both `ecosystem_earn_detail()` and `cards_earn_summary()`, so the two can't silently diverge again. Portfolio tile now shows current balance as the primary number with period-earned as a secondary "+X this QTD" line (user's choice of 3 options offered). Verified live across all 9 point ecosystems.
- **Per-person (Omer/Daniella) points balance splitting + a Total column (2026-07-24)** — Omer: track starting balances and earnings separately per person, plus account for occasional transfers between the two of them, on top of the same-day Balance Ledger feature below.
  - **`person` column added** to `Redemption`, `Transfer`, `PointsBalanceSnapshot`, `PointsAdjustment` (nullable — untagged = "Shared"), same convention as the existing `Transaction.spender` field.
  - **New `PersonPointsTransfer` model + full CRUD** (`/api/ecosystems/{id}/person-transfers`, `/api/person-transfers/{id}`) — same-currency point movement between two named people, distinct from the existing cross-currency `Transfer` model.
  - **`ecosystem_earn_detail()` rewritten** to compute a balance per bucket (each known person + "Shared" only if non-empty) instead of one combined bucket — each bucket has its own baseline snapshot, its own earned/redeemed/transferred/adjusted-since-baseline, and its own person-transfer in/out. `known_people` is server-computed (`{'Omer','Daniella'}` ∪ any other name actually used anywhere in the ecosystem), so a third person's tag is never silently dropped.
  - **Balance Ledger UI redesigned** as a table (one column per person + Shared + Total), with per-person Starting-Balance/Correction management and a "+ Transfer between us" modal/history. "Whose points" selector added to the Redemption/Transfer/Adjustment/Snapshot modals.
  - Verified live against Chase UR: existing untagged activity correctly buckets into "Shared," matching the Total column exactly; per-person modals render with the right person pre-filled.
  - Not done: no backfill of historical `Transaction.spender` — splitting only applies going forward as transactions get tagged.
- **Points ledger: editable starting balance + adjustments + full breakdown (2026-07-24)** — Omer wanted to fully reconcile an ecosystem's balance without hunting individual diffs: an editable starting balance with its date, and a manual +/- correction for drift not worth tracing.
  - **Starting Balance = the earliest `PointsBalanceSnapshot`**, no new schema — added the missing `PATCH /api/balance-snapshots/{id}` (previously only Create+Delete) and a real list UI (previously only a single "Update" button, no visibility into past entries at all despite the list endpoint already existing unused). Snapshots after the earliest now show as "Corrections," same Edit/Delete list pattern as Redemptions. The most-recent-snapshot-anchors-the-baseline calc in `ecosystem_earn_detail()` is unchanged — "Starting Balance" and "the current anchor" are just two labels over the same chronological list.
  - **New `PointsAdjustment` model + full CRUD**, mirrors `Redemption` exactly (dated, signed points delta, description, notes). Unlike a snapshot, an adjustment doesn't reset/erase history — it's a delta added into the running total from its date forward, for the "I'm off by 200 points and don't want to figure out why" case.
  - **`ecosystem_earn_detail()` now returns a `balance_breakdown`** (starting/earned/redeemed/transferred/adjusted, each since the baseline date) instead of only the final `current_balance` — these were already computed locally, just never exposed. The old single "Current Balance" stat became a full **Balance Ledger** card: Starting Balance → + Earned → − Redeemed → ± Transferred → ± Adjusted → Current Balance, each line visible.
  - Verified live against Hilton Honors: $50,000 starting balance (2025-01-01) → current balance correctly $144,219; added a real -230 adjustment via the UI → $143,989; edited and deleted both, confirmed exact round-trip back to the original $94,219 (all test data, cleaned up — unlike the prior session's challenge fixes, this ecosystem's real starting balance isn't actually known yet).
  - Found and fixed during verification: the Starting Balance row had Edit but no way to remove it back to "unset" — added a Remove button.
- **Challenges: statement-credit rewards, repeatable thresholds, txn-table jump fix (2026-07-21)** — Four items from Omer on Cards → Spend Challenges.
  - **New `bonus_type='statement_credit'`**: a challenge's payout can now be a dollar amount instead of points (`bonus_amount` is dollars). Set on the real "Retention Offer 2026" challenge (Hilton Aspire, $325) — now displays as `$325`, not `65,000 pts`, everywhere it renders, and contributes to a new `challenge_credit_total` instead of the points total.
  - **New `SpendChallenge.max_occurrences`**: a threshold can now repeat up to N times, spend resetting after each hit (confirmed with Omer: $5,500 → 11,000 pts, up to 3x, so $16,500 total spend nets all 33,000 pts). One challenge entry shows "N of 3 earned" plus a progress bar for the *current lap only* — verified the occurrence math directly (`$8,200 → 1 occurrence + $2,700 into lap 2`, `$16,500+ → capped at 3 occurrences/33,000 pts, never overflows`). Set to 3 on the real "Spend & Earn" challenge (Bonvoy Brilliant).
  - **Found and fixed while wiring the above**: the bonus-payout calculation only special-cased `bonus_type=='flat'` — everything else, including `'benefit'` (e.g. "1 free night cert"), fell through to a `per_dollar`-style `spend × amount` multiply. "Hilton Aspire FNC" (`bonus_type='benefit'`, `bonus_amount=1`) was reporting `bonus_pts_earned: 13231.9` (its raw current spend) instead of "1 reward." This exact logic was duplicated three times in `main.py` — consolidated into one `_challenge_progress()` function used by `_serialize_challenge()`, `_recalc_challenge()`, and the card-detail per-card loop, fixing the bug once instead of three times and making the two new features straightforward additions rather than three more copies of the same bug.
  - **Txn table "screen jump" on inline save, fixed**: every edit (CSC, Type, Spender, points override, Exclude, bulk-tag, teach-merchant) reloaded via `loadTxns()`, which unconditionally blanked the whole table to a spinner and back — the height swing was the jump. Added a `silent` param, used by all 9 post-edit call sites; the table now stays mounted and swaps data in place, matching the "silent reload" pattern already used elsewhere in the app (e.g. CSC edits on the main Transactions page).
  - **Redemptions/Transfers location** — answered directly (ecosystem-level, on `EcosystemDetailPage`, not per-card), no code change needed.
  - Verified live against all 4 real affected challenges (Employee Card SUB, Hilton Aspire FNC, Retention Offer 2026, Spend & Earn) — the two real corrections (statement credit, repeat count) were kept as the actual intended fix, not reverted as test data.
- **Manual points-balance snapshots + 3 more unlinked-card fixes (2026-07-20)** — Added `PointsBalanceSnapshot` (new table, full CRUD) so a manual "I checked my account, I have X points as of today" reading can anchor `current_balance` going forward — corrects for drift (unlogged promos, benefit credits, out-of-app redemptions) the computed all-time-earn math can't see. `ecosystem_earn_detail` uses the most recent snapshot as baseline when one exists (only activity after that date is added on top), falling back to all-time-earned otherwise — verified both paths directly against production data, then cleaned up the test snapshots. UI (Update button + modal on the drill-down page's Current Balance stat) landed in `v2.html` via a concurrent commit that picked up this session's uncommitted file state alongside its own unrelated Dashboard fix — confirmed nothing was lost. Separately, fixed the reported "United Explorer missing from United MileagePlus" bug: same root cause as the earlier Amex BBP case (B10) — never linked to a `CardProduct`. Audited all active cards and found 2 more silently affected the same way (both BOA Cash Rewards accounts, invisible from *every* ecosystem, not just one) — all 3 linked to their correct existing catalog products. One remaining case (Capital One VentureOne) has no matching catalog product at all, logged as B14.

- **Spender filter for shared/employee-card spend challenges (2026-07-20)** — Started from a real Amex BBP ···1008 offer requiring $4,000 of Daniella's spend specifically on her employee card, on an account she shares with Omer. Confirmed against real synced data that Plaid gives zero cardholder-level signal on shared accounts — no automated attribution is possible, so this is a manual tag.
  - New general-purpose `Transaction.spender` (free text) and `SpendChallenge.spender_filter` fields (nullable, additive migration). `_recalc_challenge()`/`_challenge_spend_for_card()` (`main.py`) both filter on `Transaction.spender == challenge.spender_filter` when set; untouched (identical `current_spend`) when not — verified against a real challenge before/after.
  - New `SearchCreateSelect` component (`v2.html`) — type-to-filter combobox with a "+ Add" row for new values, ported from MARGIN's own `frontend/src/components/SearchCreateSelect.tsx`, restyled inline to match this file's convention. Used for inline per-transaction tagging, bulk-select-and-tag (new checkbox column + bulk action bar on the Cards module's transaction table), and the challenge modal's new optional "Spender Filter" field. Bulk tagging reuses the existing `POST /api/transactions/batch-update` (a generic bulk endpoint that already existed but had no UI caller) rather than adding a new one.
  - Scoped to the Cards module's own transaction table for now (not the main Transactions page) — the backend fields are general enough for that to be a UI-only follow-up later.
  - **Two unrelated pre-existing bugs found and fixed while implementing/verifying, not part of the feature itself**: (1) the new `GET /api/transactions/spenders` route 422'd until registered ahead of `/api/transactions/{transaction_id}` — same route-ordering class of bug as the known Cash Back 422 (see MARGIN-MORESHETH-INTEGRATION.md); (2) `update_challenge()`/`create_challenge()`'s `date.fromisoformat()` couldn't parse a full ISO-datetime string, breaking edits on any challenge whose `start_date`/`end_date` happen to be stored with a `T00:00:00` suffix (discovered via the real "Employee Card SUB" challenge, which has this).
  - **Also fixed, per Omer's explicit correction**: `compute_points_earn()`'s Amex rounding used `math.ceil()` (always up) instead of rounding to the nearest dollar — switched to `math.floor(x + 0.5)` (round-half-up; avoided plain `round()`, which uses banker's rounding and would silently round some `.50`s down).
  - Verified live: tagged a real $750 transaction "Daniella," set the challenge's spender filter, confirmed `current_spend` dropped from $2,848.64 to exactly $750.00 — then cleared both the tag and the filter back to baseline afterward (test data, not the user's real records).
- **Card-level detail page fixes, using Bonvoy Brilliant ···1000 as the example (2026-07-20)** — Deep-dive session on `AccountCardDetailPage` (`v2.html`), started from "deleting a benefit fails with Benefit not found."
  - **Root cause + fix**: `seed_card_products()` (`database.py`) ran a `DELETE FROM card_benefits WHERE product_id=...` + re-insert on *every server startup* for any product with a hardcoded benefits list. Since `BenefitUsage.benefit_id` cascades on delete, this silently wiped all logged usage history and handed out new ids every restart/deploy — the actual cause of "Benefit not found" (any id the browser had could go stale the moment the server restarted), not a bug in the delete endpoint itself. Replaced with an upsert-by-name (find-or-create, update in place, never delete) — verified ids and usage stable across two consecutive restarts, and delete now works and stays deleted across restarts (a seed-defined benefit that's deleted gets cleanly re-added by name on the next restart, by design).
  - **New `tracking_type` field** (`periodic` vs `by_use`) on `CardBenefit`, migrated via the existing lightweight `required_columns` mechanism (no Alembic in this repo) with a one-time name/frequency-based backfill. Distinguishes real use-it-or-lose-it credits (the $25/mo dining credit) from by-use/non-expiring ones (Global Entry every-4-yrs credit, $100 on-property credit — both were previously mislabeled `reset_frequency='annual'`, which also made the UI wrongly show "Resets Dec 31" for something that doesn't reset annually — fixed).
  - **Multi-period usage tracker**: `periodic` benefits with a sub-annual frequency (monthly/quarterly/semi-annual) now get a checkbox-style grid (Jan–Dec, Q1–Q4, or H1/H2) in `GET /api/cards/{id}/benefits`'s response instead of a single current-cycle progress bar — one-click toggle marks/un-marks a period at full value, reusing the existing `PUT /benefits/{id}/usage` / `DELETE /benefit-usage/{id}` endpoints (no new write paths). Groundwork for a future "benefit expiring soon" alert — not built yet, by design.
  - **`by_use` benefits stay visible as before** (single card, Log Usage button, no grid/reset-date, no future alert eligibility) — a manual "Tracking Type" dropdown was added to the Add/Edit Benefit modal for corrections. `card-research` skill (`~/.claude/skills/card-research/SKILL.md`) updated to classify by-use/non-expiring benefits into Key Notes instead of the trackable Benefits & Credits table when researching new cards.
  - **Transactions table custom date range**: `GET /api/accounts/{id}/transactions` gained `start_date`/`end_date` (takes precedence over year/month/quarter); added a 4th "Custom" toggle alongside Monthly/QTD/YTD with two date inputs.
  - **Click a Spend Challenge → auto-filters Transactions to its date range** (a challenge's window rarely aligns to a calendar month/quarter, e.g. 2026-06-03–2026-09-01) — shows a "Filtered to challenge: {name} ✕" chip; editing the date inputs afterward overrides it and clears the chip, satisfying "still allow the custom filter."
  - Found but explicitly not fixed, logged as B12: the Benefits & Credits summary line mixes annual and current-period basis in its "remaining" figure.
- **Amex BBP missing from Portfolio/ecosystem drill-down + `earn-summary`/`earn-detail` totals diverging (2026-07-19)** — User reported "Amex Blue Business Plus 1008" missing from the Amex MR ecosystem drill-down (it did show in the Your Cards strip, just not attributed to any ecosystem). Root cause: account 135 had `product_id`/`ecosystem_id` both `NULL` — never linked to a `CardProduct`, so every ecosystem-attribution query silently excluded it. The correct catalog product already existed (`amex_blue_business_plus`, id 32) but the auto-suggestion banner had proposed the wrong one ("Amex Platinum", medium confidence) — see B10. Linked account 135 → product 32 via the existing `/api/accounts/{id}/link-product` endpoint. While verifying, found a second bug: `/api/cards/earn-summary` (powers the Portfolio tiles) and `/api/ecosystems/{id}/earn-detail` (powers the drill-down) disagreed on point totals for the same account/period — e.g. Amex BBP showed +1,524 pts on the Portfolio tile vs -146 pts on the drill-down. Cause: `earn-summary` used a flat SQL `SUM(amount)` aggregate with its own hand-rolled Expense/Income filter, never migrated to `compute_points_earn()` (the single authoritative per-transaction function, with Amex per-dollar rounding, `points_earn_override`, and payment-description detection already fixed there in earlier sessions). Rewrote `earn-summary`'s non-`auto_top_category` path to fetch transaction rows and call `compute_points_earn()` per-row, matching `earn-detail` exactly — verified byte-for-byte agreement across all 9 ecosystems (Amex MR, Marriott Bonvoy, Chase UR, etc.) after the fix. Deliberately left the `auto_top_category` branch (Citi Custom Cash-style 5%-on-top-category cards) untouched, since `earn-detail` doesn't have an equivalent calculation to match against — logged as B11.
- **Classification-engine consolidation (deployed 2026-07-18, 8-commit sequence)** — Redesign of transaction auto-classification, planned at `/Users/omermorashti/.claude/plans/shiny-zooming-conway.md`. Goal: every classification traceable to one specific active `CategorizationRule` or sitting in the review queue — nothing silently inferred with no visible trace, the architecture class that caused the Best Buy regression (see B6 below). Shipped in dependency order, each step verified against live production data before the next:
  1. Deleted the dead `MerchantOverride` table/endpoints (0 rows, unreachable from any UI — confirmed before removing).
  2. Deleted `learn_from_corrections()`/`_check_transfer_correction()` entirely (not just patched). Ran a read-only diff first: 97 transactions depended on them — reviewed each merchant with the user and created 5 explicit rules to replace the inference before deleting it (Pruplazacafe, Stop & Shop, AMC Theatres, Ellaola, Keshetbroad; 2 of these — Stop & Shop, AMC — were actually **mis**-inferred as "Business" and got corrected to Groceries/Entertainment in the process). Also removed a second, separate call to the same correction method inside Plaid sync (`_sync_item`) that would have thrown `AttributeError` on the next sync if missed.
  3. Centralized `needs_review` threshold logic (5 independently-duplicated, drifted inline formulas) into one `compute_needs_review()` helper.
  4. Added non-blocking overlap/duplicate warnings at rule-creation time, plus a one-time audit of all 532 active rules — found 20 overlapping pairs, logged as B9.
  5. Scoped `_reapply_rules()` to the triggering rule's own pattern instead of a full-table rescan on every rule create/update (previously confirmed to touch unrelated transactions as a side effect even from a throwaway rule); added a `dry_run` flag as permanent tooling; also wired a reapply into `create_rule_from_transaction`, which previously didn't retroactively catch backlog matches at all.
  6. **Course-corrected mid-implementation**: the original plan called for collapsing the two-pass rule matcher into one pass. A dry-run diff against all 2140 real transactions showed that would silently un-categorize ~387 of them — about a quarter of the rule set relies on a genuine two-stage "normalize the description, then categorize the normalized name" pattern that a single raw-text-only pass breaks. Kept the two-phase structure (it's load-bearing, not incidental complexity); shipped only the three sub-fixes a dry-run diff confirmed were actually safe (0 unintended differences): Phase-1 first-match-wins made consistent across both action and description (previously action was first-wins, description was last-wins, same loop), Transfer-clears-category centralized into `categorize()` itself (removing a snippet duplicated at 7 call sites — caught because the user correctly pushed back that a credit-card payment shouldn't carry a spend category at all), and deterministic rule ordering via the previously-unused `priority_order` column plus a pattern-length tiebreak (51 rules currently share priority=100 with no defined secondary order).
  - **Not fixed by this pass, logged as B9**: 12 of the 20 audited rule-overlap pairs need a human decision — 8 "general beats specific by an adjacent priority number" bugs (a specificity-based algorithm change was tested and confirmed to fix none of them) plus 3 true contradictions and 1 same-priority tie.
  - **Deliberately deferred, not started**: making LLM enrichment automatic on ingestion (today it's a manual "AI Enrich" button) — separable scope with its own cost/behavior tradeoffs, worth revisiting once the queue size after this pass is known.
- **B6 fixed — `_check_transfer_correction()` over-generalization (deployed 2026-07-18)** — Root cause: the word-overlap safeguard counted merchant-name words as valid overlap, so a correction whose description was just the merchant name (e.g. "BEST BUY") became a no-op guard — it always matched, because every transaction from that merchant contains the merchant name too. Now excludes merchant-name words from the overlap set on both sides. Also fixed the actual delivery mechanism: `_reapply_rules(force_unlock=True)` (runs on every rule creation, `main.py:7446`) gated its locked-row loop on "does a rule match" but then applied the *full* `categorize()` output, including the separate correction-override — decoupled by adding an `apply_corrections` flag to `categorize()`, set to `False` in that branch so it only ever applies what its own gate check confirmed. Restored v2.html's missing GCB/Rule/Exclude row-action buttons (frontend.html had them, v2.html didn't) in the same pass — the Rule button matters beyond convenience since it's the only path that turns an LLM classification into a reusable rule. Surfaced `times_rejected` in the Rules table (previously tracked but not displayed) with a red ⚠ when rejections outnumber acceptances — immediately found B7 plus B8 (see above). **Caution for future sessions**: the local dev server points at the real production Postgres DB (`.env` `DATABASE_URL`, no separate test DB) — creating *any* rule unconditionally triggers a full reapply pass across all ~1800 transactions and ~530 rules, so even a throwaway test rule has real side effects. Verify against a non-mutating endpoint first where possible.
- **B3 fixed — Transactions page Expenses total diverging from KPI (deployed 2026-07-18)** — Root cause confirmed against live data across every month: under default settings the Transactions-page footer and the Dashboard KPI (`/api/stats`) already matched exactly. The one reproducible divergence was toggling "Show excluded" — its own tooltip says excluded transactions stay "hidden from totals," but the footer's `_budgetVisible` was built from the same list used for row display, so toggling it back into view also pulled it back into the sum (confirmed: two benefit-credit transactions caused an exact $550 gap for July). `_budgetVisible` now always excludes `is_excluded` rows regardless of the toggle. Also ported the Cards module's Redemptions/Transfers CRUD UI (already live in `v2.html`) into `frontend.html`'s `EcosystemDetailPage` — backend endpoints already existed, this just wired the UI; verified end-to-end (modal open/close, ecosystem dropdown population, live points-received calc).
- **Points-earn accuracy: P2P exclusion + Amex per-dollar rounding (deployed 2026-07-18)** — New "P2P Payments" CSC auto-excludes Venmo (and future Cash App/etc.) from points and SUB; backfilled 56 existing Venmo transactions. `compute_points_earn()` now rounds up to the nearest dollar before multiplying, scoped to Amex-issued cards only (verified: $4.66 × 7x → 35 pts, not 32.6). Found and fixed a real regression along the way: a stale learned correction was blanket-flipping every Best Buy transaction to `action='Transfer'`, exposed by triggering `_reapply_rules(force_unlock=True)` — reverted the 8 affected transactions. Underlying over-generalization bug in `_check_transfer_correction()` still needs a real fix — see PLAN.md.
- **Cards module: font fix + txn table Action/Exclude controls (deployed 2026-07-17, round 2)** — Fixed 92 `DM Sans` references to a font that was never loaded (fell back to OS default), app-wide. Added inline Action-type editing and an Exclude/Include toggle to the Cards module's own transaction table. Found and fixed two related bugs: `/api/accounts/{id}/transactions` was hard-hiding excluded transactions instead of dimming them, and `row-excluded`/`row-locked`/etc. CSS classes were referenced but never defined anywhere in `v2.html`.
- **Points/Transfers overhaul (deployed 2026-07-17, commit `3fb7f33`)** — Redemption/Transfer/TransferRatio split, `compute_points_earn()` rewritten as a deterministic pure function (fixes 4 previously-inconsistent earn-rate call sites), Cash Back drill-down 422 fixed, Transfers UI + classification-aware points display ported into `v2.html`. Coordinated with the sibling MARGIN project via `MARGIN-MORESHETH-INTEGRATION.md`. Outstanding: manual `is_excluded` cleanup pass on large clawback transactions (benefit credits).
- **V2 redesign full QA pass** — Two passes across all 11 pages: visual (fixed 3 crashes + systemic ~20-class CSS gap breaking modals/headers) then functional (clicked every filter, modal, toggle, and drill-down). Found and fixed the circular `--blue-primary`/`--blue-vibrant` CSS variable bug (broke 72 usages app-wide) and the Daily Balances 30d/90d toggles silently ignoring their range. `v2.html` is now fully operational.
- **Multi-select filters** — Type, Category, Account dropdowns on Transactions page now support multiple selections
- **Account type capitalization** — IRA, HSA, FSA, CD, 401(k) display correctly everywhere
- **Expense credit-netting** — `/stats` and `/budget/actuals` both net refunds in expense categories
- **KPI card fix** — Reverted broken income reclassification, re-applied with correct category_type filter
- **Moresheth branding** — Full rebrand: gold coin logo, Playfair Display, PWA icons/splash, manifest, favicon
- **Cash Flow rework** — Scoped to checking/savings/cash accounts only, categorized breakdown
- **Account reclassification** — Dropdown to change account types (HSA/FSA from "other" to "savings")
- **Lock icon alignment** — Moved to far right with fixed-width spacer for consistent row layout
- **Budget credit-netting** — Income-action transactions in expense categories offset expenses
- **Cards earn rate** — Product lookup checks both account.product_id and card.product_id
- **CSC edit silent reload** — Points estimate updates after CSC change without full page refresh
- **Refresh preserves state** — refreshKey prop pattern instead of key={refreshKey} destroying components
- **Mobile GCB tagging** — Toggle in mobile transaction edit modal
- **Multi-card challenge explosion** — Per-card entries with individual spend tracking
- **Dashboard "See more"** — setPage prop passed to DashboardPage
- Cards Landing Page (ecosystem overview, per-card challenges, earn summary)
- Benefits & Credits CRUD, merchant CSC teaching, inline CSC editing
- CSC filter + points summary, monthly/QTD/YTD toggle
- Challenge bonus in Earn Summary, comprehensive NOT NULL migration
