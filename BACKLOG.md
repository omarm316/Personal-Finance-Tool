# Moresheth — Backlog

> Maintained collaboratively. Update priorities here at the start of each session.
> Status: 🔴 Bug/Blocker · 🟡 High Priority · 🔵 Queued · 💡 Nice-to-Have

---

## 🔴 Active Bugs / Needs Verification

| # | Item | Notes |
|---|------|-------|
| B1 | **Challenges: NOT NULL constraint errors** | `product_id`, `required_spend`, `reward_value` — migration deployed; verify no new columns surface |
| B2 | **Challenges: "cannot load challenge" error** | `db.rollback()` fix deployed; verify challenge list loads cleanly |
| B7 | **Venmo mis-tagged "Healthcare" budget category** | A stray auto-created rule (`id=504`, "Auto-created from LLM enrichment") sets `set_category='Healthcare'` for any VENMO-pattern transaction — 5 rejections, 0 acceptances in its history, suggesting it's been silently wrong and manually corrected repeatedly. Found 2026-07-18 while adding the P2P Payments CSC. **Still not fixed** — now also visible directly in the Rules table (Settings → Rules) as "5m 0a 5r ⚠". A second, previously-undocumented bad Venmo rule was also found at priority 340 ("9m 0a 9r ⚠") — see B8. |
| B8 | **Rule cleanup pass — several rules with 100% rejection history** | Found 2026-07-18 once `times_rejected` was surfaced in the Rules table (Settings → Rules → STATS column, red ⚠ when rejected > accepted). Confirmed still active with 0 acceptances against real rejections: priority-340 Venmo rule (9m 0a 9r, duplicates B7's problem), `AUTOPAY` action rule (8m 0a 8r), `CHASE CREDIT CRD` (3m 0a 3r), `CITI AUTOPAY;CITI CARD ONLINE PAYMENT` (2m 0a 2r), `VERIZON` (2m 0a 2r), `TRANSFER` description rule (2m 0a 2r). Needs a human pass — each rule needs its own judgment call (fix pattern vs. deactivate vs. leave as duplicate-but-harmless), not a blanket delete. |
| B9 | **Rule overlap audit — 12 pairs need a manual priority/pattern fix** | Found 2026-07-18 via the new `find_overlapping_rules()` audit (part of the classification-engine redesign, see PLAN.md). 20 overlapping-pattern pairs found total; 8 are already correctly resolved today (specific pattern already has the lower priority number) and need no action. The other **12 need a manual fix, none of them self-resolving**: **8 "general pattern accidentally beats a more specific one by an adjacent priority number"** bugs — `UBER`(5233) beats `UBER EATS`(5234), `KINGS`(5120) beats `KINGS OF KOBE`(5121), `MOUNT SINAI`(5142) beats `MOUNT SINAI PARKING`(5143), `COSTCO`(5064) beats `COSTCO GAS`(5065), `HILTON`(5100) beats `HILTON RESORT CREDIT`(5101), `CC PAYMENT`(40) beats `CHASE CC PAYMENT`(200, id 524), and `HOT`(5107, a coincidental substring) beats both `TEANECK ROAD HOT BAGELS`(5218) and `WDW PHOTOPASS`(5246) — **correction to the note originally logged here**: a "prefer more specific pattern" reorder was tested directly during the redesign and confirmed to fix *none* of these — it only breaks ties between rules sharing the exact same priority number, and these all involve different (adjacent) priorities, so each needs its specific priority number changed by hand. Plus **3 true contradictions**: two `CHASE CC PAYMENT` rules (id 14 → Transfer vs id 524 → Fees & Interest — 524 is dead), two `AUTOMATIC PAYMENT` rules (id 528 → Transfer vs id 529 → Expense — 529 is dead), `ALIGNED INCENTIV` vs `ALIGNED INCENTIVES` (id 500 vs id 65, looks like a typo variant). Plus **1 same-priority tie**: `SCHEELS ALL SPORTS` vs `SCHEELS ALL SPORTS IFARGO` (ids 522/523, both priority 200, currently resolved by non-deterministic Postgres row order). |
| B10 | **Card→product auto-suggestion algorithm gives bad matches** | Found 2026-07-19 while investigating why "Amex BBP 1008" (account 135) was missing from every ecosystem drill-down (see Completed). The `/api/accounts/product-suggestions` fuzzy-matcher suggested "Amex Platinum" for it at only medium confidence, even though an exact catalog match (`amex_blue_business_plus`, official_name "Blue Business Plus Card") already existed — that's likely why the suggestion was never confirmed, leaving the account permanently unlinked. **2026-07-20: found and fixed 3 more instances of the same pattern** (see Completed) — United Explorer 9968 (suggested "United Quest" instead of the near-exact "United℠ Explorer Card" match) and both BOA Cash Rewards accounts (7384, 8344 — both suggested "Fidelity Rewards Visa" instead of the exact "Bank of America Customized Cash Rewards" match). All 4 now manually linked to their correct products. **Still not fixed: the scoring function itself** — it's not surfacing exact/near-exact official_name matches over generic ones, so this class of bug will keep recurring for any newly-linked account until the algorithm itself is fixed. One account (CapitalOne VentureOne 9689) surfaced during the 2026-07-20 audit has no correct suggestion available at all since no matching catalog product exists — see B14. |
| B12 | **Benefits & Credits summary mixes annual/period basis in "remaining"** | Found 2026-07-20 on the Bonvoy Brilliant card page. The card-level summary line ("$X annual value · $Y used · $Z remaining") sums `annualValue(b)` (full annualized value, e.g. $300/yr for a $25/mo credit) into "annual value" but sums only the *current period's* `remaining` (e.g. $25, since the monthly credit resets monthly) into "remaining" — so the two numbers are on different bases and don't reconcile (e.g. "$520 annual value" vs "$245 remaining" looked like $275 was unaccounted for). Not a data bug, just a confusing display — flagged to the user, not fixed (out of scope for the card-level session that found it). Would need either an amortized "remaining this year" calc across all periods, or relabeling to make the period mismatch explicit. |
| B18 | **West Elm 6184 + Fidelity Visa Signature still have no `Card` row** | **The recurring cause is fixed (2026-08-10, see Completed) — this entry now tracks only the two pre-existing orphans, not new ones.** `_ensure_cards_for_new_accounts()` now runs after every account sync (first link and "+ Add Account") and auto-creates a `Card` row for each brand-new credit-card `Account`, so this class of gap won't recur for cards linked from now on (verified against the still-unlinked Citi Strata Premier prep, see PLAN.md). Still manual: **West Elm 6184** (4 txns, no product — blocked on **F12**, the West Elm Key Rewards research) and **Fidelity Visa Signature** (acct 159, `product_id=23` already set, 0 transactions) — both predate the fix, and it deliberately only acts on newly-created accounts (not a backfill sweep) so it can't interact with B31's still-open duplicate account. |
| B20 | **Two visually-similar "Other" buckets in Points by Category** | Found 2026-07-26 on the Amex MR page. `by_cat` in `ecosystem_earn_detail()`/`cards_earn_summary()` defaults untagged transactions (`points_category IS NULL`) to the literal string `'Other'`, but there's also a real, distinct catalog category named **"Other / Uncategorized"** (`PointsCategory` id 13) that transactions can be explicitly tagged with — so the UI can show both an "Other" row (untagged) and an "Other / Uncategorized" row (explicitly tagged) side by side, which reads as a confusing near-duplicate. Still not fixed — visible again on the redesigned Amex MR page 2026-07-31, user deferred again. Would need either renaming the code's fallback label (e.g. to "Untagged") or merging the two concepts. |
| B29 | **Amex Business Gold's 4x-top-2-categories bonus isn't modeled** | Added the product 2026-07-31 (see Completed) at its accurate 1x base rate only — the real mechanic (4x on whichever 2 of 6 categories get the most spend *that billing cycle*, up to $150K/yr combined) doesn't fit the existing `auto_top_category` engine (built for Citi Custom Cash: top-1 category, $500/month cap). Until a proper top-N/annual-cap engine exists, this card's points will under-report by the missed 4x bonus on ~2 real categories. Documented in the product's seed comment (`database.py`) rather than modeled incorrectly. |
| B27 | **4 pre-existing failures in `tests/test_earn_rates.py`** | Found 2026-07-30 while verifying the B26 perf work. `TestProductLookup::test_account_product_id_used`, `TestProductLookup::test_card_product_id_fallback`, `TestCashBackEarnRate::test_cash_back_accumulates`, `TestParentCategoryFallbackIntegration::test_l2_falls_back_to_l1` all assert a nonzero `points_earned` and get **0**. **Confirmed pre-existing, not caused by the B26 refactor** — `git stash`ed the changes and got the identical 4 failures (4 failed / 7 passed both ways). Almost certainly fallout from the 2026-07-25 locked-points architecture: points are now computed once at write time by `_lock_points_for_transaction()` and read from `Transaction.points_earned`, but these tests create fixtures and expect the old live-compute read path, so nothing ever populates the locked column. The tests are stale, not the code — but they should either be updated to lock points in their fixtures or be rewritten against the current design, because right now the earn-rate suite gives no real signal. Rest of the suite is green (53 passed). |
| B25 | **Two different "total expenses" on screen at once, $299 apart** | Found 2026-07-27 during the Dashboard review. In Annual YTD the Expenses KPI reads $83,445.79 (from `/api/stats` `total_expenses`) while the Budget Performance Total reads $83,146.79 — the difference is exactly the $299.00 of expense-side transactions categorized `Work`, which `/api/stats` counts as an expense but `BudgetAndSpendingCard`'s `SKIP` set excludes (correctly — `Work` is the income bucket). Neither number is wrong on its own terms; they're just on different bases with no indication of it. Needs a decision on which basis the dashboard should present, or a footnote — not a code bug. Worth pairing with a look at why $299 of spend is categorized `Work` at all. |
| B30 | **Two bank connections silently lost accounts via Plaid "+ Add Account"** | Found 2026-08-08 while investigating "Amex BBP 1008 has no new transactions" — turned out not to be a sync bug. Live-compared every connection's Plaid `accounts_get()` response against the DB and found **American Express (Omer)** now reports only 1 of its 6 accounts (`Business Gold Card`) and **Chase (Omer)** reports 9 of 10 (missing `Hyatt 1677`) — the other accounts were silently de-authorized, not erroring, so `last_error_code` stays null and syncs report "0 added" cleanly forever. Root cause confirmed via `created_at`: `Business Gold Card 1000` (account 167) was added 2026-07-31, two days after BBP's last real transaction (07-29) — classic Plaid Update-Mode gotcha, where re-opening Link to add one new account re-presents the *full* account checklist and treats anything left unchecked as revoked, not "leave as-is." Chase's dropped `Hyatt 1677` doesn't have the same "new account" signature (all 10 Chase accounts share the original 2026-03-04 link date) — either a separate re-auth Update-Mode session left it unchecked, or the card was closed on Chase's side. **Fix (user action only — needs live Plaid Link, can't be done from the backend):** Settings → Connected Banks → "+ Add Account" on each connection, and re-check **every** previously-linked account, not just the new/missing one: Amex BBP 1008, Hilton Honors 1008, Amex Platinum 1008, Hilton Aspire 1009, Amex HY Savings 1820 (Amex Omer); Hyatt 1677 (Chase Omer, after confirming the card is still open). No data will be lost or duplicated once reconnected — confirmed live via a full cursor-reset resync on the Amex item that all 7 previously-Excluded transactions on that connection survived untouched, and existing transactions are matched by content hash. **Preventive note for future "+ Add Account" use:** always re-check every existing account in the Plaid Link checklist, not just the new one being added. **Correction 2026-08-10, see B31: the "no data lost or duplicated" claim above does not hold when two accounts on the same item share a mask** — BBP's reconnect actually resumed Platinum under a mislabeled duplicate account, and BBP itself is still unfixed. |
| B31 | **B30's fix mislabeled Platinum as BBP; BBP is still frozen** | Found 2026-08-10 re-investigating "does BBP have August transactions" after the B30 fix. **Account 135 (`Amex BBP 1008`) is still fully frozen — zero transactions since 2026-07-29, the "+ Add Account" flow never actually restored it.** What looked like recovery was **Amex Platinum (account 137)** resuming sync under a new `plaid_account_id` — but the app's account-matching fell through to *creating a new row* (account 168, labeled `Blue Business Plus Card 1008`) instead of reattaching to account 137, because **BBP and Platinum share the identical last-4 mask (`1008`)** on the same Amex item and the matcher's institution+mask+type fallback (`main.py` ~1741-1774, the `account_hash` step) can't tell them apart when neither existing row has a null `plaid_account_id` to match against. Verified by content: all 104 transactions in account 168 dated ≤2026-07-27 are exact duplicates (date+amount+merchant) of transactions already correctly in account 137; only 14 (2026-08-02 through 08-08) are genuinely new, and those are unlocked (`points_earned`/`card_id` both null, since 168 was never given a `Card` row). **Platinum side fixed 2026-08-10** via a one-off transactional DB script (scratchpad, not committed to the repo — this was a one-time data repair, not app code): deleted the 104 duplicate transactions on account 168, moved the 14 real new ones (2026-08-02 → 08-08) to account 137 with `card_id=7` and relocked their points, pointed account 137's `plaid_account_id` at the live value, cleared it on 168, and deactivated 168. Verified post-fix: account 137 has 118 transactions (2025-12-05 → 2026-08-08, zero duplicates), account 168 has 0 transactions and `is_active=false`. **2026-08-11: attempted the BBP reconnect fix, and it forked two more.** Nulling account 135's `plaid_account_id` and running "+ Add Account" did not restore BBP — the account actually re-checked was Hilton Honors (easy to mis-click: the Plaid Link checklist shows multiple `••••1008` lines with no visual distinction beyond the card name), and it forked **yet another** mislabeled account (172, labeled `Blue Business Plus Card 1008`) instead of matching the real Hilton Honors row (136), this time because of a **different**, compounding bug: the exchange/sync code writes `account_type` from Plaid's subtype in **lowercase** (`main.py` ~1800-1802), but existing rows were normalized to **capitalized** form (`'Credit Card'`) by an unrelated migration — so even with 136 unlinked, the institution+mask+type fallback's exact-match filter (`main.py:1841`) can't find it. This is the same case-sensitivity trap already called out in this file's Architecture Notes for `account_type` (see B5) — the account-matcher was never using a case-insensitive comparison. **Note found in the code itself while diagnosing this** (`main.py` ~1825-1827): a *previous* session had already identified "the Amex 1008 collision" and added an "only match unlinked accounts" guard specifically because of it — that guard is necessary but not sufficient; it still requires the account_type casing to agree, and requires nulling only the *one* target account before each individual reconnect attempt (nulling multiple at once reintroduces ambiguity between them).

Content-verified and fixed the same way: **account 172 → merged into 136** (all 23 transactions were exact duplicates of existing Hilton Honors data, nothing new — pure delete + repoint, no migration needed). **Also found and fixed two more forks from the original 2026-08-08 reconnect, same root cause:** **account 169 → merged into 139** (Amex HY Savings — all 12 transactions were exact duplicates), and **account 170 → merged into 138** (Hilton Aspire — messier: 124 of 152 transactions were duplicates, but 28 were genuinely new real spend through 2026-08-04, and the account carried a *different* mask, `2007` vs the original `1009`. User confirmed this is a real card reissue after a lost card, not a mislabeling bug — fixed by migrating the 28 unique transactions in, deleting the 124 duplicates, and updating account 138 itself to the new mask/name (`Hilton Aspire 2007`) and live `plaid_account_id`, rather than treating 170 as disposable). All four merges used the same pattern: classify each duplicate account's transactions as exact-content-match (delete) vs genuinely new (migrate + relock points), verify counts before and after, single atomic DB transaction, no repo code changes (one-off scratchpad scripts, not committed).

**BBP is still the one open item — needs user action.** Before the next attempt: null out account 135's `plaid_account_id` again (it's already null as of the last attempt, but confirm), then in the Plaid Link checklist **identify the row by the card name "Blue Business Plus Card," not by the digits** — every other `••••1008` row should already be correctly linked at this point (Platinum, Hilton Honors both fixed), so BBP should be the only remaining unlinked 1008-masked account, but the casing bug means even a clean single-candidate match isn't guaranteed. Verify immediately after by content-comparing whatever account results, the same way all four bugs above were caught. **BBP fixed 2026-08-11 — B31 closed.** A further reconnect attempt actually checked BBP this time, and it forked one more mislabeled duplicate (account 173, same casing bug), but this time it carried the real thing: content-verified zero overlap with Hilton Honors/Platinum/Aspire, 56/88 transactions exactly matching account 135's existing history via a precise per-row check (a naive JOIN-based count overcounted at 56 due to duplicate-content fan-out — worth remembering that aggregate JOIN counts on content-match keys can inflate past the true distinct-row count when values repeat, as they did here with recurring $150 "Advanced Therapy" charges), 36 genuinely new (2026-07-31 → **08-09**). Merged the same way: 52 duplicates deleted, 36 migrated to account 135 with `card_id=35` and points relocked, `plaid_account_id` repointed, account 173 deactivated. **Verified live: account 135 now has 111 transactions, 2026-01-27 → 2026-08-09, real August data present** (Bloomingdale's, Mystic Aquarium, Walmart, recurring "Ivyrehab Pt"/"Bh Backup" charges). All five forked accounts from this saga (168, 169, 170, 172, 173) are now merged and deactivated; all five real accounts (135, 136, 137, 138, 139) are live with correct `plaid_account_id`s.

**The account_type casing mismatch (exchange-time lowercase vs. stored capitalized) is a real, unfixed code bug, left open** — it caused every one of the five forks above, and will keep causing this exact failure mode (duplicate/mislabeled account instead of a clean re-link) for any account on any item, any time Plaid rotates a `plaid_account_id` during Update Mode, until the comparison is made case-insensitive or the two write paths agree on one casing convention. Worth fixing directly in code rather than continuing to patch data by hand. |

---

## 🟡 High Priority

| # | Item | Notes |
|---|------|-------|
| H6 | **UI tightening pass — "airy", Monarch-informed** | Brief from Omer 2026-07-30: tighten layouts on mobile and desktop; **menus and color scheme**, not the palette. Concrete spec written to [docs/ui-direction.md](docs/ui-direction.md) from the reference shots in `assets/example_images/`. Headline: what makes Monarch feel airy is **less color and less enclosure**, not more whitespace — neutral chrome with one accent per screen, hairline borders instead of shadow/glass, uniformly outlined controls, value-first KPI typography, consistent ~50px row rhythm. **Decided: the 24px in-bar-label budget bars from 2026-07-27 stay** (Monarch's thin bars were explicitly rejected). Sequenced *after* the Vite Phase 2 split, since most of these changes touch every page and are far cheaper against extracted components. One open decision flagged in the doc: whether to adopt the two-column masonry dashboard. |
| H1 | **Mobile QA pass** | User working through mobile-specific bugs — awaiting findings |
| H2 | **Account reclassification** | User should reclassify FSA/HSA accounts from "Other Assets" to correct type using the new dropdown |
| H3 | **Transaction: filter by description/merchant** | Text search input — already exists; verify working well |
| H4 | **Challenge: verify recalc correctness** | Confirm spend from `activation_date` → today captured correctly |
| H5 | **Card Research Skill — capture benefits** | Extract full benefit list and auto-POST to `/api/card-products/{id}/benefits` |

---

## 🔵 Features — Queued

| # | Item | Notes |
|---|------|-------|
| F1 | **Classify merchant with AI** | "Ask AI" button next to null-CSC merchants — Claude web-searches MCC/category → saves as merchant mapping |
| F2 | **Plaid re-link safety** | No duplicate transactions or broken accounts when re-linking a Plaid item |
| F3 | **Benefits: auto-track from transactions** | If a benefit has `trigger_category`, auto-update `amount_used` from matching transactions |
| F4 | **Challenges: suggestions / templates** | "Suggested challenges for your cards" — backend exists, wire to UI |
| F5 | **Visa/MC merchant category API** | Use Visa Supplier Locator API to look up merchant MCCs; map MCC → CSC |
| F6 | **Network-specific CSC overrides** | Per-network merchant mapping for earn rate accuracy |
| F7 | **Points valuation** | CPP multiplier per ecosystem to show estimated $ value alongside raw points |
| F8 | **Add Ritz-Carlton `CardProduct` + run the real Boundless→Ritz-Carlton migration** | The `POST /api/cards/{card_id}/change-product` feature (2026-07-25) is ready, but there's no Ritz-Carlton entry in the card-products catalog yet — a concurrent session's stopgap only swapped the card-art image on the existing `marriott_bonvoy_boundless` product_key. Needs a card-research pass for Ritz-Carlton's real earn rates/benefits, then run `change-product` for real on the two actual Bonvoy Boundless cards (card_id 31, 32) with the real effective date. |
| F12 | **Research West Elm Key Rewards card** | To-do only, not yet researched — flagged 2026-07-26 alongside AA Platinum Select (that half done same day, see Recently Completed). Card Management's "Auto-detected Product Matches" banner suggests `West Elm 6184`→Fidelity Rewards Visa — worth checking against real research rather than confirming blind. |

---

## 💡 Nice-to-Have / Phase 2

| # | Item | Notes |
|---|------|-------|
| ~~N1~~ | ~~**PWA v2**~~ | ✅ Pull-to-refresh, swipe actions, skeleton loading, virtual scroll, offline support — all shipped |
| N2 | **Component refactor** | Extract frontend.html into proper component files |
| N3 | **Export to CSV/PDF** | CSV export exists; add PDF support for statement reconciliation |
| N4 | **AI-driven UI polish** | Use v0/Lovable for isolated components once functionality stable |

---

## ✅ Recently Completed

- **B18's recurring cause fixed — new cards now get a `Card` row and an accurate catalog `status` automatically (2026-08-10)** — Omer asked how to make sure his newly-acquired Citi Strata Premier (already in the catalog as `citi_strata_premier`, id 44, prepped 2026-07-24 per PLAN.md, but not yet linked via Plaid) would show up automatically once synced, rather than needing another manual fixup like Business Gold/Bilt/AA Platinum Select before it.
  - **`_ensure_cards_for_new_accounts()`** (`main.py`) now runs right after account reconciliation in both `exchange_public_token` (first-time Plaid Link) and `_sync_item_background`'s `clear_cursor` branch (Update Mode / "+ Add Account" on an existing item) — the two places that create `Account` rows. It auto-creates the matching `Card` row for each brand-new credit-card account, guessing `issuer` from the institution name and deriving a unique `card_id` from the account name (collision-suffixed if needed). **Deliberately scoped to only the accounts created in that same call**, not a sweep of every Card-less credit account on the item — B31 documents a still-open duplicate/mislabeled account (168) sharing a mask with a real one, and a blanket backfill would hand that duplicate a permanent `Card` row and complicate its planned cleanup. Pre-existing orphans (B18's West Elm/Fidelity cases) are unaffected and stay manual.
  - **`CardProduct.status` ('active'/'not_held') was cosmetic and never updated** — `/api/accounts/{id}/link-product` and `/api/cards/{id}/change-product` set `account.product_id`/`card.product_id` but never touched `status`, so the badge on the card detail page could say "not_held" for a card the user actively held (and vice versa after unlinking). New `_refresh_product_held_status()` recomputes it from whether any `Account` or `Card` still references the product, called after every link/unlink/change. Also fixed a latent inconsistency found while doing this: unlinking an account only cleared `account.product_id`, never the linked `Card.product_id`, so the two could drift — unlink now clears both, matching the dual-write convention `change-product` already used.
  - **Confirmed live** (read-only queries against the production DB): the catalog entry, card art, and earn rates for Citi Strata Premier are all already in place; no `Account` row exists yet because Omer hasn't linked it via Plaid. Once he does, the existing `/api/accounts/product-suggestions` fuzzy-matcher plus the new auto-Card-creation should take it end-to-end to "shows up on the Citi ThankYou page" with just a one-click Confirm, no manual DB step.
  - **Flagged, not fixed here**: B30's Plaid Update-Mode "+ Add Account" checklist gotcha still applies — adding the Strata Premier account on the Citi connection requires re-checking every existing Citi account (Double Cash 8475, Custom Cash 3240, AA Platinum 1855, Citi Checking 6816) in the Plaid Link picker, or they risk silent de-authorization.
- **B19 fixed + Amex MR cover-banner redesign + Amex Business Gold onboarded (2026-07-31)** — Second pass on the Amex MR ecosystem page, this time implementing the cover-banner/card-art mockup Omer approved. Found B19's actual root cause while wiring it up: `by_card_out` in both `ecosystem_earn_detail()` and `cash_back_earn_detail()` (`main.py`) never included `product_key` — the frontend's card-art/gradient lookup (`EcosystemDetailPage.jsx`) was keyed on it, so it always resolved to an empty string and silently fell back to a blank gray box on **every** ecosystem page, not just Amex MR. Added `product_key` to both response builders.
  - **New hero/cover banner** replacing the old plain center-stat block: ecosystem logo badge, a capped fan of real card art (top 3 by points this period, "+N more" chip beyond that — so it scales automatically as cards are added, no per-card code needed), current posted balance as the primary figure with the pending line beneath it (amber, reuses the existing pending-points feature), QTD earn as a secondary line. Along the way, fixed Amex MR's badge/hero color — it was `#059669` (green), a real mismatch against the actual blue Amex brand and card art, now the correct blue.
  - **"Your Cards" upgraded from a row list to a tile grid** at full art opacity (previously `opacity:0.4`, a second, independent cause of the art looking washed out even when it loaded) — falls back to the product's brand gradient with the card name overlaid when no art file exists yet, rather than a blank box.
  - **Onboarded a genuinely new account found mid-conversation**: Omer had synced a new "Business Gold Card 1000" Amex account with no catalog match. Researched the real American Express® Business Gold Card via the card-research skill (web search, not memory) and added it (`amex_business_gold`) — $375/yr, 1x base, 4 real periodic benefits (FedEx/Grubhub/office supply $20/mo, Walmart+ $12.95/mo, Squarespace $150/yr, ChatGPT Business $300/yr) plus the Hotel Collection by-use credit. The card's real 4x-on-top-2-categories earning mechanic doesn't fit the existing single-category `auto_top_category` engine (built for Citi Custom Cash) — modeled at 1x base only rather than fabricating a wrong bonus, logged as **B29**. Linked the account, created its missing `Card` row (same "sync doesn't auto-create one" gap as Bilt/AA Platinum Select), relocked its 6 transactions.
  - Verified live in both themes: hero renders correctly with the real 4-card fan (3 shown + "+1"), Business Gold's gradient-fallback tile shows its name legibly, no console errors, Portfolio tile page (a separate component, untouched) still unaffected.
  - **Next**: same hero pattern to be extended to the other ecosystem pages (Chase UR, Marriott Bonvoy, Citi ThankYou, etc.) — the two backend fixes (`product_key`, the color-mismatch pattern) already apply everywhere; only the frontend hero/tile-grid rollout remains per-ecosystem.
- **B4 + B28 fixed — the Transactions page went from "No transactions found" to 464 rows in 1s (2026-07-30)** — B28 was the visible symptom of B4, so they were fixed together.
  - **B4's recorded cause was wrong.** The backlog said "page loads trigger a synchronous full Plaid sync across all banks." Checked every `@app.get` route for sync calls: **none triggers a Plaid sync.** The real cause was ordinary N+1 lazy-loading, found by profiling rather than reading:
    - **`/api/accounts`: 98 queries / 7.1s → 3 queries / 0.31s.** `list_accounts` called `get_account_balance()` per account, and that helper re-fetches the `Account` it was handed the id of *and* runs a per-account `SUM` — 49 account fetches + 48 sums. New `get_account_balances_bulk()` does it in one grouped query, expressing the same anchor model set-wise (`date(t.date) > date(a.start_date)` is exactly equivalent to the scalar version's end-of-anchor-day comparison). Accounts with a *future* anchor take the scalar walk-backward branch, which has no set-wise equivalent, so they fall back to the per-account path. **Verified against all 48 real accounts: zero mismatches, 6.97s → 0.07s.**
    - **`/api/transactions?limit=500`: 70 queries / 5.5s → 8 queries / 1.0s** (6.8s → 0.87s over HTTP). Two lazy-load N+1s: `_serialize_txn` reads `t.account.account_name`/`account_type` (37 queries) — fixed with `contains_eager` on the join the query *already had*, so it costs nothing extra, unlike `joinedload` which would add a second join; and `r.points_category.name` in the rate-map builder (26 queries) — fixed with `joinedload`.
  - **B28's three fixes**, all real independent faults:
    1. `TransactionsPage.load()` used `Promise.all` for transactions + accounts, so a failure on **either** discarded **both**. Now `Promise.allSettled`, each applied on its own merits — and since accounts only feeds filter labels, it can no longer blank the table.
    2. `sw.js` answered a failed `/api/` fetch with an empty **array** (`[]`) at status 503. Any caller reading the body before the status saw a valid "no results" payload, so a transient failure rendered as a confident empty list. Now returns an explicit error **object** that can never be mistaken for a successful empty collection. `CACHE_VERSION` → `v13`.
    3. The empty state didn't distinguish "no results" from "failed to load" — both rendered "No transactions found". Now a separate error state with the message and a Retry button.
  - **Verified live**: Transactions renders **464 TRANSACTIONS in ~1 second** (previously "No transactions found" indefinitely), no console errors.

> Older completed work (through 2026-07-26) lives in
> [docs/archive/BACKLOG-completed-through-2026-07-26.md](docs/archive/BACKLOG-completed-through-2026-07-26.md).

- **B26 fixed — `/api/cards/earn-summary` 43s → 9s (2026-07-30)** — profiled rather than assumed, which was worth it: the endpoint issued **596 queries totalling 41.3s of SQL**, and because `DATABASE_URL` points at a *remote* Railway Postgres, essentially all of it was ~70ms round-trip latency rather than computation. The backlog's suspected cause (per-account `CardProduct` lookups) was real but only **2.7s of 43s**. Four fixes, in descending order of impact:
  1. **`calc_auto_top_category_points()` issued one query per calendar month** while walking its range — and `_compute_balance_bucket()` calls it with `start_date=2000-01-01` whenever a bucket has no baseline snapshot. So a single auto-top account (Citi Custom Cash) cost **~318 round-trips**, Jan 2000 → today, to compute points for an account with ~2 years of data. That alone was ~24s. Rewritten to one query for the whole range, bucketed by `(year, month)` in Python — months with no activity contribute nothing, so skipping them is equivalent. Deliberately preserved the original's quirk of starting at `start_date.replace(day=1)` (it included transactions earlier in the start month) rather than silently narrowing the window.
  2. **`_compute_ecosystem_balance()` re-queried transactions and snapshots once per *person bucket*** — and the helper itself runs once per ecosystem, so ~12 ecosystems × ~3 buckets fanned out badly. Every per-bucket filter is on `spender`/`card_id`/date, all trivially evaluable in Python, so both are now fetched once per ecosystem and partitioned in memory. **Subtle trap handled**: `Transaction.date` is a `timestamp` but `snapshot_date` is a `date` — comparing them directly in Python is a `TypeError`, and truncating `t.date` to a date would silently drop same-day-after-midnight transactions the SQL counted. Uses `datetime.combine(baseline, time.min)` to match SQL's coercion exactly.
  3. **The five per-ecosystem ledger queries** (redemptions, transfers out/in, adjustments, person-transfers) plus a snapshot-people query and a per-ecosystem `Card` lookup — 6+ round-trips per ecosystem — are now one batched `IN` query each, grouped in Python. The `Card` lookup needed no query at all; `card_by_acct` was already in scope.
  4. **The per-account `CardProduct` N+1** the backlog originally identified — batched into one `IN` query via a new `_load_products_by_id()` helper. The same pattern existed in **three** handlers (`cards_earn_summary`, `cash_back_earn_detail`, `ecosystem_earn_detail`), all fixed.
  - **Correctness proven by diff, not by inspection**: captured the full JSON response for all three periods (`mtd`/`qtd`/`ytd`) plus six ecosystem drill-downs, `git stash`ed the changes, captured the same battery from the original code, and diffed recursively. **Zero differences.** The battery itself went 130.3s → 27.5s, confirming `ecosystem_earn_detail` benefits too (shared helper). Profiled result: **596 queries → 121, 42.2s → 9.3s.**
  - Ties into **B4**: this was one of the worst offenders, but the sync-on-every-page-load issue is still open.
- **B5 fixed — `/api/cash-flow` returned all zeros (2026-07-30)** — prioritized because Omer asked to dive into the Cash Flow and Daily Balances screens next, and this bug made those screens useless. Root cause was a **one-line case-sensitivity bug**, not the suspected scoping/tagging problem: `get_cash_flow()` (`main.py`) filtered `Account.account_type.in_({'checking','savings','money market','cd','cash','hsa','fsa'})` — all lowercase — but `account_type` is stored **capitalized** (`'Checking'`, `'Savings'`, `'HSA'`, `'FSA'`). The `in_()` matched **zero** accounts, `cash_id_list` came back empty, and the endpoint short-circuited to its `empty` return — which is exactly why every month and every range toggle read $0 with `transaction_count: 0`. Fixed with `func.lower(Account.account_type).in_(...)` so it can't rot again if Plaid changes capitalization. Verified: 0 accounts matched before → 10 after; July 2026 returns 64 txns, $21,571.96 in / $28,537.68 out, and `by_inflow`/`by_outflow` each sum exactly to those totals. Checked June 2026, YTD 2026 and full-year 2025 — all return real data. Cash Flow & Forecasting page now renders populated KPIs, a 6-month bar chart and both breakdown tables. (Related note: the elaborate both-capitalizations workaround in the Net Worth `GROUP_ORDER` map, `main.py` ~11340, exists because of this same inconsistency — worth normalizing `account_type` at write time someday.)
- **B14 closed as stale — VentureOne catalog entry already exists (2026-07-30)** — audited against production after Omer mentioned holding the card. `capital_one_venture_one` (id 46) is present and `active`, account 152 (`VentureOne 9689`) is linked to it, and it has a proper `Card` row (card 17, product 46, ecosystem 4, primary_user Omer). This was done during the 2026-07-26 B15 work; the B14 row was simply never closed. No code or data change needed.
- **🔴 Mobile scrolling was completely broken in portrait — app-wide (2026-07-30)** — Omer, mid-session: "scrolling does not work on my iphone, only when i rotate it to landscape." Not a Dashboard bug; **every page** was affected. Root cause: the `@media (max-width:768px)` block sets `.app-container { flex-direction: column }`, which makes height the main axis. `.main` had `min-width:0` (the fix for the row axis) but **not** `min-height:0`, and its `overflow` is `visible`, so its automatic minimum size (`min-height:auto`) resolved to its full content height — it grew straight past `.app-container`'s fixed `height:100%`. `.content`'s `flex:1` then resolved against that inflated height, so the scroll container ended up exactly as tall as its own content (`scrollHeight === clientHeight`) and could never scroll, with the excess clipped by `.app-container`'s `overflow:hidden`. Landscape is >768px, stays `row`, and was unaffected — which is exactly why it only ever reproduced in portrait. Note `.content` itself was fine all along: `overflow-y:auto` zeroes an item's automatic minimum size, which is why the same latent flaw never bit on the row axis. **One-line fix**: `min-height: 0` on `.main`. Measured before/after at 375×812: `.main` 1412px → 812px, `.app-container` scrollHeight 1412 (600px clipped and unreachable) → 812 (nothing clipped), `.content` scrollHeight 1348/clientHeight 1348 → 1096/748, and a programmatic `scrollTop=400` that previously went nowhere now lands at the true max of 348. Re-verified on Transactions/Cards/Accounts/Budgets — `.main` bounded and nothing clipped on all of them.
- **Dashboard cleanup — B22/B23/B24 + the `isMob` resize bug (2026-07-30)** — the four items logged during the 2026-07-27 Dashboard review, all in `DashboardPage`/`v2.html`:
  - **B22 — mobile Top Budgets no longer shows "$X left" for unbudgeted categories.** Mirrored the desktop `RowEl`'s flags (`hasBudget`/`isCredit`/`noBudget`/`over`). An unbudgeted row now renders an empty track with a "no budget" tag and reads "$4,060.50 spent · no budget set" instead of "$4,060.50 left". Also aligned `over` to the desktop's `>=100` (was `>100`, so exactly-100% rows disagreed between the two views) and suppressed the meaningless negative % on credit rows. **Found a second live instance the backlog hadn't caught** — "For Others" ($947.45), same bug. All six rendered rows cross-checked against `/api/budget/actuals` + `/budget/targets`: category order, percentages and labels match exactly.
  - **B23 — 11 dashboard requests per load → 8.** Dropped the selected-range `/stats` call (fed `stats`/`topCats`, neither rendered), `/transactions?needs_review=true&limit=50` (fed `reviewQ`; the Needs Review section was removed), and prior-year `/budget/targets` (fed `budgetTargetsPrior`). Prior-year *actuals* is kept — the Spending Trend's 6-month window reaches into `viewYear-1`. Removed the now-dead state, plus the unused `expCats` local and `getDates()`, which became dead once `statsQ` went. Verified by reading the network log on a clean (service-worker-cleared) load. Helps **B4** — one of the dropped calls pulled 50 transactions.
  - **B24 — KPI cards now 2-up on mobile.** Media-query override on `.metric-grid`, scoped `:not(.grid-3):not(.grid-4)` so other pages' deliberate fixed-column grids are untouched; padding/type scaled down to suit the narrower column. Measured actual text widths: widest value is 99px against 133px available, no wrapping, no page-level horizontal overflow.
  - **`isMob` resize bug** (was logged as a second, duplicate **B26** — the number collided with the `/api/cards/earn-summary` item, which stays open). Replaced the bare `window.innerWidth<768` render-time read with the file's existing `useIsMobile()` hook, which already subscribes to `resize` on the identical breakpoint. Verified the round trip: at 1440px the mobile Top Budgets card is gone, at 375px it's back, driven purely by the resize event.
  - **Verification note**: the preview browser's `resize_window` (CDP device-metrics override) updates CSS media queries but does **not** dispatch a `resize` event to the page, so React state hooks won't react to it. This initially looked like the `useIsMobile` fix had failed. Dispatch `new Event('resize')` manually to test resize-driven React behavior. Real device rotation does fire it.
  - Verified live at 375×812 and 1440×900 in both themes; no console errors beyond the usual benign Babel "deoptimised" notice (whose *presence* confirms the compile succeeded).
- **Card detail: annual fee vs. credits (2026-07-28)** — the Annual Fee KPI tile on the card detail page now nets the general `Fees & Interest` category (how Omer classifies both the fee and any credits he redeems) within the current anniversary-anchored fee cycle (`Card.issue_date`'s month/day, falls back to calendar year if unset), showing e.g. "$358 net · $895 fee · $537 credits" instead of just the sticker fee. Clicking the tile drills into the Transactions table filtered to that category + cycle window, reusing the existing challenge-click-to-filter pattern with a new independent `category` filter dimension (`account_transactions()` gained a `category` param, separate from the existing CSC/`csc` param). Verified live against real fee/credit data on Amex Platinum 1009. See PLAN.md for the full writeup; B26 (slow `/api/cards/earn-summary`) was found but not fixed along the way.
- **Dashboard layout pass — budget bars, split trend/transactions row (2026-07-27)** — Five changes Omer asked for after the review pass above:
  1. **Budget Performance bars rescaled to 0–100% of each row's own budget**, with subtle markers at 25/50/75. Replaces the shared-scale-plus-budget-tick approach (which the 2026-07-24 proposal artifact had introduced) — at a shared scale a position meant something different on every row, so fixed quarter markers weren't possible. Over-budget rows now fill the track and carry their true figure in the label (458% and 101% both fill; the number disambiguates).
  2. **Bars thickened to a 24px track with the % label inside**, matching the "after" panel of Omer's proposal artifact (`.bud-track.tall` — 24px, 6px radius, 1px border, label right-aligned at 10.5px/700). Row padding tightened 12px→10px to compensate.
  3. **"INCOME" indicator removed.** Credit rows (negative actual — refunds/reimbursements) now render an empty track with no bar, no markers and no % label; the green `+$X` in the Actual column already conveys it. Also suppressed the nonsense negative % these rows were showing (Education read "-17%" of a $4,330 budget).
  4. **Spending Trend and Recent Transactions are now a 50/50 row** (new `.dash-split` grid, stacking to one column under 900px). Y-axis value labels dropped from the chart — every point is already direct-labeled, so the axis duplicated information while eating width the card no longer has; month labels kept, since nothing else conveys them. Chart left padding 44→24 accordingly. New `RecentTransactionsCard` shows the five most recent transactions with a "See more →" that fades in on card hover (0.18s), is `tabIndex={-1}`/`aria-hidden` while invisible, and pins visible on `(hover: none)` devices so touch users can still reach it.
  5. **Bottom full-width Recent Activity table removed**, superseded by (4).
  - **Two contrast/weight corrections made during live verification, not in the original spec**: (a) the proposal's soft fills (`--red-soft` at 0.16 alpha) were designed for *short* over-budget bars under a shared scale — at a 100% scale every over-budget row fills the whole track, so the alarm rows were reading as the calmest on screen. Over/near fills now weighted heavier than under-budget (0.42/0.38 vs 0.30) using base color + opacity, so the four `-soft` tokens added earlier in the session were removed again as unused. (b) The % label is right-aligned to the *track*, so on anything under 100% it sits on empty track, not on the fill — `--blue-soft`/`--amber` there fails contrast in light theme. Only over-budget rows keep a status color now; the rest use `--text-secondary`.
  - **Verified live in both themes at 1440px and 375px**: sort order now runs cleanly 458→296→225→163→132→130→87→76→73→36→32→22→7→0 with unbudgeted rows below every budgeted one, "See more" fades in/out and navigates to Transactions, `.dash-split` collapses to a single 343px column on mobile, and the old Recent Activity table is gone from the DOM.
  - **Note for next session — service worker caching bit hard here.** After editing `v2.html` the browser kept running a stale copy (`sw-v11` shell cache), producing a `RecentTransactionsCard is not defined` error for code that was plainly in the file — the served HTML was correct, the *cached* HTML wasn't. Confirmed by checking `navigator.serviceWorker.controller` and searching the live `<script type="text/babel">` text for the new identifier. Fix: unregister all SW registrations and `caches.delete()` every key, then reload. Worth checking first whenever a v2.html edit appears not to have taken effect.
- **Dashboard review pass — Spending Trend fixed, fake KPI badge removed (2026-07-27)** — Omer: "let's look closely at the Dashboard page." Read the page live (desktop + mobile) and cross-checked every displayed figure against the API. Three real problems found and fixed, all in `DashboardPage` (`v2.html`):
  - **Spending Trend was plotting spending + income.** `getMonthTotal` summed *every* key in `budgetActuals` with no filter, while the rest of the dashboard skips `Transfer` and `Work` — and `Work` is the income category. Every point was inflated by that month's income, and the *shape* was wrong, not just the scale: the chart showed March as the peak with a steady decline since, when real spending was **lowest** in March and peaked in May. Verified against `/api/budget/actuals` (chart vs. true spend: Feb 24.3k→10.3k, Mar 41.7k→8.2k, Apr 33.2k→11.6k, May 33.1k→19.8k, Jun 32.1k→18.1k, Jul 16.1k→7.0k). The July point also disagreed with the Budget Performance Total sitting directly below it on the same screen. Now applies the same `SKIP` set as `BudgetAndSpendingCard` and the mobile bars — one shared declaration at the top of the block (the mobile-bars section had a second `const SKIP` in the same scope that had to be removed at the same time, or the duplicate `const` is a syntax error that blanks the whole app — see PLAN.md's note about Babel compile failures presenting as a blank page).
  - **Spending Trend ignored the year selector.** `months6` was built from `new Date()` while the data it indexed came from the payload fetched for `viewYear` — so selecting 2025 silently plotted 2025 figures under Feb…Jul **2026** labels. Confirmed live before fixing (the chart flatlined to zero while the axis still read Feb…Jul). The window now ends on the selected month (`endM = viewYear<curY ? 12 : min(viewMonth, curM)`, clamped so it never trails into future months), so the label range and the payload year always agree. This also makes the chart respond coherently to the month selector — picking Mar 2026 gives an Oct 2025–Mar 2026 window that correctly reads across both the `viewYear` and `viewYear-1` payloads. Added a range label ("Feb – Jul 2026" / "Oct 2025 – Mar 2026") to the card header, since a cross-year window's bare month names are ambiguous.
  - **"Checking & Savings" % badge was fabricated.** There's no historical per-bucket balance available, so the prior balance was being estimated as `current − net cash flow`. That isn't a prior checking balance (net cash flow spans every account, including card spend that never touched checking), and it went absurd once the estimate crossed zero — Annual YTD rendered **"+583%"** in green, styled identically to the genuine Income/Expenses badges. Badge dropped (now "—", matching the Net Worth card). Dead `invBal`/`invPct` (the same formula, computed but never rendered) removed with it. A real figure needs a per-bucket historical balance from the backend — `get_account_balance(db, id, as_of_date=...)` already exists and `/api/net-worth/timeline` uses it, but the timeline endpoint only returns aggregate assets/liabilities/net worth, not the `Cash & Savings` bucket, so surfacing a real % is a backend change (and B4 makes adding another per-account-per-month computation to page load a bad trade until that's addressed).
  - Verified live at each step against the API: post-fix chart reads 10.3k/8.2k/11.6k/19.8k/18.1k/7.1k (exact match), Dec 2025 renders 5.0k against an actual 4,983.13 (and would have shown 17.9k pre-fix), the Mar 2026 cross-year window matches Dec 4,983.13 / Jan 8,152.48 / Feb 10,301.74 / Mar 8,158.99, the Expenses KPI and the chart's rightmost point now agree, and the mobile Top Budgets card still renders correctly after the `SKIP` consolidation. No console errors.
